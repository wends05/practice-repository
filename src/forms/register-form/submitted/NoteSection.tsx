import { Card, CardContent } from "@/components/ui/card";

export default function NoteSection() {
  return (
    <Card>
      <CardContent>
        <h4>Note</h4>
        <p>
          Please save the QR code above. You will need it for event check-in.
        </p>
      </CardContent>
    </Card>
  );
}
